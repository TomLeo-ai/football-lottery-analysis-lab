# OCR Core

Placeholder for browser local OCR interfaces and helpers.

Default OCR behavior must be local-first. Optional server OCR adapters must be disabled by default and must not publish OCR output as official public data.

